{
  "name": "disastermesh-sentinel-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix dashboard run dev",
    "build": "npm --prefix dashboard run build",
    "test": "npm --prefix dashboard run test"
  }
}
