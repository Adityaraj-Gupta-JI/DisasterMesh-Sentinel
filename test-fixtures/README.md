# Test fixtures

Generated by `python3 scripts/make_fixtures.py` from the real reporting pipeline,
so a fixture can never describe behaviour the code does not produce.

| File | Purpose |
|---|---|
| `demo-medical-incident.json` | P0 medical/collapse report used in the demo |
| `demo-flood-incident.json` | Flood report for the boat-dispatch path |
| `demo-multilingual-incidents.json` | Tamil and Hindi reports |
| `demo-routine-incident.json` | P3 logistics report (must never pre-empt P0) |
| `demo-evidence.jpg` | Placeholder attachment bytes |
